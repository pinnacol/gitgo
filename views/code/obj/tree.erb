<h2>Tree <%= format.sha_a sha %></h2>
<div class="tree">
  <table><% obj.contents.each do |content| %>
  <tr>
  <td><%= content.name %></td>
  <td><%= format.sha_a content.id %></td>
  </tr><% end %>
  </table>
</div><%= render_comments(sha) %>