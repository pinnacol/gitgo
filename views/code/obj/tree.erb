<h2>Tree <%= escape_html(sha) %></h2>
<div class="tree">
  <ul class="contents"><% obj.contents.each do |content| %>
    <li class="<%= content.kind_of?(Grit::Blob) ? 'blob' : 'tree' %>"><%= show_link(content) %></li><% end %>
  </ul>
</div><%= render_comments(sha) %>