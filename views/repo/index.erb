<h1>Gitgo Repo</h1>

<dl>
<dt>Path</dt>
<dd><%= format.path path %></dd>
<dt>Branch</dt>
<dd><%= format.branch branch %></dd>
<dt>Commit</dt>
<dd><% if commit.nil? %>
  <em>Select a tracking branch and click setup to create the '<%= format.branch branch %>' branch.</em>
  
  <form action="<%= form.url('repo', 'setup') %>" method="post">
    <select id="remote" name="remote"><% form.each_remote_name(remote) do |value, selection, content| %>
    <option value="<%= value %>"<%= html.select(selection) %>><%= content %></option><% end %>
    </select>
    <input type="submit" value="Setup" />
  </form>
<% else %><%= erb :_commit, :locals => {:commit => commit}, :views => path('views/code'), :layout => false %><% end %></dd>
<dt>Update</dt>
<dd>
  <p>
    Synchronize with a remote (pull and push), or simply pull updates.
  </p>
  <form action="<%= form.url('repo', 'update') %>" method="post">
    <dl>
    <dt><label for="remote">Remote</label></dt>
    <dd>
      <select id="remote" name="remote"><% form.each_remote_name(remote) do |value, selection, content| %>
      <option value="<%= value %>"<%= html.select(selection) %>><%= content %></option><% end %>
      </select>
    </dd>
    <dt><label for="push">Sync</label></dt>
    <dd><input id="sync" type="radio" name="sync" value="true" checked="true" /></dd>
    <dt><label for="pull">Pull</label></dt>
    <dd><input id="pull" type="radio" name="sync" value="false" /></dd>
    <dt><input type="submit" value="Update" /></dt>
    <dd></dd>
    </dl>
  </form>
  <p><em>Note: updates can fail silently, for example when the remote cannot be reached.</em></p>
</dd>
<dt>Reset</dt>
<dd>
  <p>
    Reinitialize the internal bindings gitgo uses to access the git repository.
    Uncommitted updates in gitgo (see <a href="<%= form.url('repo', 'status') %>">status</a>)
    will be lost.
  </p>
  <form action="<%= form.url('repo', 'reset') %>" method="post">
    <input type="checkbox" name="full" value="true">Reindex</input>
    <input type="submit" value="Reset" />
  </form>
</dd><% if session %>
<dt>User Branch</dt>
<dd>
  <p>The user branch is determines which gitgo documents are active.  User branch defaults to the one checked out in the working directory.</p>
  <form action="<%= form.url('repo', 'session') %>" method="post">
    <select id="ref" name="ref"><% form.each_ref_name(at) do |value, selection, content| %>
    <option value="<%= value %>"<%= html.select(selection) %>><%= content %></option><% end %>
    </select>
    <input type="submit" value="Change" />
  </form>
</dd><% end %>
</dl>

<ul>
<li><a href="<%= format.url('/repo/status') %>">Status</a></li>
<li><a href="<%= format.url('/repo/idx') %>">Index</a></li>
<li><a href="<%= format.url('/repo/maintenance') %>">Maintenance</a></li>
<li><a href="<%= format.url('/repo/reference') %>">Reference</a></li>
</ul>
