#!/usr/bin/env ruby

begin
  require 'gitgo'
rescue(LoadError)
  # assume development if gitgo cannot be found
  require "rubygems"
  require "bundler"
  Bundler.setup(:default)
  
  require 'gitgo'
end

Gitgo::Server.start
